# frozen_string_literal: true

RSpec.describe ConnectFour2307 do
  it "has a version number" do
    expect(ConnectFour2307::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
